{
  "name": "zrx-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ redbooker/zrx-token",
    "create-local": "graph create --node http://localhost:8020/ redbooker/zrx-token",
    "remove-local": "graph remove --node http://localhost:8020/ redbooker/zrx-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 redbooker/zrx-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}